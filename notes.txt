06/01/2026

From Slack messages:
1 - we're encountering this size bug, afaik this points at a user error on our end, this should only happen very rarely
2 - do we need to copy the binaries?
3 - related to 2, we should implement the saturation detection
4 - we need to implement unit curation. It looks like https://spikeinterface.readthedocs.io/en/stable/modules/curation.html has some things, from a quick read, their spike quality metrics are probably not quite what we want but probably a good start

From what I remember from the meeting too:
- need a way to remove bad channels
- should look on Discord and note anything useful
- definitely new ways to do things


Notes from reading the Discord:

- Bill Karsh's CatGT https://html-preview.github.io/?url=https://github.com/billkarsh/CatGT/blob/main/CatGT-linux/CatGT_ReadMe.html
    - Apparently it allows you to concat data to make this easier
- Mention of ecephys 
- Should probably ensure we're using latest spikeinterface
- python-neo https://github.com/NeuralEnsemble/python-neo



CatGT - tshift, filter, common-average-ref, removes artifacts, extracts TTL events ect.

Qs:
- don't understand how to combine datasets
- what is g and t ? 
- what are ni and ob streams?
- do folders need to just have data in it?

Okay so it looks like I can't use CatGT without imec files which is annoying... I wanted the speed :/ 

- Add in way to detect bad channels
    - remove from KS 



Now looking into the KS fails
- seeing the same shanks having the issue
    - makes me think there aren't spikes being picked up or there are too many

Looking at one shank (npx10 a0) drift scatter looks good for days it ran then when the signal gets too small it just can't run

So points back to looking for methods to detect bad channels

Then using bad channels to decide whether to run it at all? 

maybe:

- use 20 min chunk
- find bad channels
- if too many bad channels don't run 
- flag that data quality poor
